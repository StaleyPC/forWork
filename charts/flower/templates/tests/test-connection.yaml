apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mayan.fullname" $ }}-test-connection"
  labels:
    {{- include "mayan.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mayan.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
